'''
what is langchain : Langchain is a library where it is used to use the pretrained
                    models of LLM or train our own data with llm.
                    -> Langchain is a library which is used to use the built_in_functions
                    which are available inside the library only


what is langchain_community : langchain_community is also a library where we can use its own built_in_funtions and also
                              we can take other built_in-Functions use API calling



***As as developer if you want to use pretrained LLM from openai or ollama or groq simply
we can use langchain and do it =>  but if we want to train the LLM with our own data we need to follow the pipeline

that is called as RAG architecture

RAG = Retrival Argumented Generation



What is RAG Pipeline:

[ Load_data ]  => [Chunks] => [Embeddings] => [VectorDB]

'''